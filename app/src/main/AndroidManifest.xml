<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lxp.widgetview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".common.activity.CommonActivity"
            android:windowSoftInputMode="adjustResize|adjustPan" />
        <activity android:name=".activity.ViewActivity" />
        <activity android:name=".timer.activity.TimeViewActivity" />
        <activity android:name=".drag.activity.DragActivity" />
        <activity android:name=".draw.activity.DrawActivity" />
        <activity android:name=".bezier.activity.BezierActivity" />
        <activity android:name=".bezier.activity.OneBezierActivity" />
        <activity android:name=".bezier.activity.TwoBezierActivity" />
        <activity android:name=".inflate.activity.InflateTestActivity" />
        <activity android:name=".zoom.activity.ZoomImageActivity" />
        <activity android:name=".zoom.activity.ImageTypeActivity" />
        <activity android:name=".zoom.activity.WideImageActivity"></activity>
    </application>

</manifest>